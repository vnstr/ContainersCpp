#include <iostream>
#include <vector>
#include <queue>

#include "../ft_vector/ft_vector.hpp"
#include "ft_queue.hpp"

int main()
{
	// std::queue< int, std::vector<int> > std_queue;
	// ft::Queue< int, ft::Vector<int> >   ft_queue;

	// std::cout << "\nEmpty()" << std::endl;
	// std::cout << "std: " << std_queue.empty() << std::endl;
	// std::cout << "ft : " << ft_queue.empty()  << std::endl;
	// std::cout << std::endl;

	// std::cout << "\nsize()" << std::endl;
	// std::cout << "std: " << std_queue.size() << std::endl;
	// std::cout << "ft : " << ft_queue.size()  << std::endl;
	// std::cout << std::endl;

	// std::cout << "\npush(10)" << std::endl;
	// std_queue.push(10);
	// ft_queue.push(10);
	// std::cout << std::endl;

	// std::cout << "\nfront()" << std::endl;
	// std::cout << "std: " << std_queue.front() << std::endl;
	// std::cout << "ft : " << ft_queue.front()  << std::endl;
	// std::cout << std::endl;

	// std::cout << "\nback()" << std::endl;
	// std::cout << "std: " << std_queue.back() << std::endl;
	// // std::cout << "ft : " << ft_queue.back()  << std::endl;
	// std::cout << std::endl;

	// std::vector<const int> arr(10, 2);
	// std::vector<const int> arr2(arr.begin(), arr.end());

	// ft::Vector<int> arr;
	// ft::Vector<int> arr2(10, 2);

	// arr.insert(arr.begin(), arr2.begin(), arr2.end());

	// for (size_t i = 0; i < arr.size(); ++i) {
	//     std::cout << arr[i] << " ";
	// }
	// std::cout << std::endl;


	return 0;
}
